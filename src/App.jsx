// src/App.jsx
import React from "react";

/**
 * App component
 * 
 * Routing is handled by RouterProvider in main.jsx
 * Layouts are handled in routes.jsx (MainLayout)
 */
const App = () => {
  return (
    <div className="w-full h-full">
      {/* 
        App-level logic can go here later:
        - Global error boundaries
        - Toast notifications
        - Modals
        - Auth guards
      */}
    </div>
  );
};

export default App;
